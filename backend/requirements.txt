fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
requests==2.32.3

email-validator==2.2.0

# Server-side speech-to-text (tarteel-ai/whisper-base-ar-quran)
# CPU-only torch build — Render's free tier has no GPU, and the default
# PyPI torch wheel on Linux pulls several GB of unused CUDA libraries
# (nvidia-cudnn-cu12, nvidia-cublas-cu12, etc.) if you don't point it here.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
transformers==4.45.2
accelerate>=0.26.0
av==13.1.0
numpy==1.26.4

